---
import BaseLayout from '../layouts/BaseLayout.astro';
import metrics from '../data/metrics.json';

// Categorize metrics
const categories = {
  impact: {
    label: 'Impact & Scale',
    color: 'blue',
    icon: '📊',
    metrics: metrics.all.filter(m => ['years_experience', 'identity_mau', 'architecture_reviews', 'high_stakes_programs'].includes(m.id))
  },
  ai: {
    label: 'AI Enablement',
    color: 'emerald',
    icon: '🤖',
    metrics: metrics.all.filter(m => ['ai_enabled_engineers', 'ai_weekly_adoption', 'ai_champions'].includes(m.id))
  },
  feedback: {
    label: 'Stakeholder Feedback',
    color: 'purple',
    icon: '💬',
    metrics: metrics.all.filter(m => m.id.startsWith('feedback_'))
  }
};

// Theme colors for feedback bars
const themeColors = {
  'Collaboration': { bg: 'bg-emerald-500', text: 'text-emerald-600 dark:text-emerald-400' },
  'Technical depth': { bg: 'bg-blue-500', text: 'text-blue-600 dark:text-blue-400' },
  'Innovation': { bg: 'bg-violet-500', text: 'text-violet-600 dark:text-violet-400' },
  'Helpful & available': { bg: 'bg-amber-500', text: 'text-amber-600 dark:text-amber-400' },
  'Strategic vision': { bg: 'bg-cyan-500', text: 'text-cyan-600 dark:text-cyan-400' },
  'Execution': { bg: 'bg-rose-500', text: 'text-rose-600 dark:text-rose-400' },
  'Positive energy': { bg: 'bg-pink-500', text: 'text-pink-600 dark:text-pink-400' },
  'Leadership': { bg: 'bg-indigo-500', text: 'text-indigo-600 dark:text-indigo-400' }
};

const feedbackThemes = metrics.all.filter(m => m.id.startsWith('feedback_theme_'));
const feedbackMeta = metrics.all.filter(m => m.id.startsWith('feedback_') && !m.id.startsWith('feedback_theme_'));
---

<BaseLayout title="Proof Method" description="How the metrics on this site are measured and validated.">
  <section class="py-12 md:py-16">
    <div class="max-w-3xl mx-auto px-6">
      <h1 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-4">
        How I measure
      </h1>
      
      <p class="text-lg text-gray-600 dark:text-gray-400 mb-8">
        Receipts over rhetoric means showing work. Here's how each number is sourced.
      </p>

      <!-- Methodology -->
      <div class="mb-12 p-6 rounded-xl bg-gray-50 dark:bg-gray-800/50 border border-gray-200 dark:border-gray-700">
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Methodology</h2>
        <div class="grid gap-4 md:grid-cols-3">
          <div class="flex items-start gap-3">
            <span class="text-2xl">📈</span>
            <div>
              <span class="font-medium text-gray-900 dark:text-white block">Dashboard exports</span>
              <span class="text-sm text-gray-600 dark:text-gray-400">Admin dashboards, analytics tools</span>
            </div>
          </div>
          <div class="flex items-start gap-3">
            <span class="text-2xl">🔧</span>
            <div>
              <span class="font-medium text-gray-900 dark:text-white block">Internal tools</span>
              <span class="text-sm text-gray-600 dark:text-gray-400">Feedback systems, org trackers</span>
            </div>
          </div>
          <div class="flex items-start gap-3">
            <span class="text-2xl">🔍</span>
            <div>
              <span class="font-medium text-gray-900 dark:text-white block">Keyword analysis</span>
              <span class="text-sm text-gray-600 dark:text-gray-400">Theme extraction from text</span>
            </div>
          </div>
        </div>
      </div>

      <!-- Impact & Scale -->
      <div class="mb-10">
        <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
          <span>{categories.impact.icon}</span> {categories.impact.label}
        </h2>
        <div class="space-y-3">
          {categories.impact.metrics.map((metric) => (
            <div class="p-4 rounded-lg bg-white dark:bg-gray-800/80 border border-gray-200 dark:border-gray-700 shadow-sm">
              <div class="flex justify-between items-center">
                <span class="font-medium text-gray-900 dark:text-white">{metric.label}</span>
                <span class="text-xl font-bold text-blue-600 dark:text-accent">{metric.value}{metric.unit && ` ${metric.unit}`}</span>
              </div>
              <div class="mt-2 flex flex-wrap items-center gap-2 text-xs">
                <span class="px-2 py-0.5 bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 rounded">
                  {metric.method}
                </span>
                {metric.proof_note && (
                  <span class="text-gray-500">{metric.proof_note}</span>
                )}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- AI Enablement -->
      <div class="mb-10">
        <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
          <span>{categories.ai.icon}</span> {categories.ai.label}
        </h2>
        <div class="space-y-3">
          {categories.ai.metrics.map((metric) => (
            <div class="p-4 rounded-lg bg-white dark:bg-gray-800/80 border border-gray-200 dark:border-gray-700 shadow-sm">
              <div class="flex justify-between items-center">
                <span class="font-medium text-gray-900 dark:text-white">{metric.label}</span>
                <span class="text-xl font-bold text-emerald-600 dark:text-emerald-400">{metric.value}{metric.unit && ` ${metric.unit}`}</span>
              </div>
              <div class="mt-2 flex flex-wrap items-center gap-2 text-xs">
                <span class="px-2 py-0.5 bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-300 rounded">
                  {metric.method}
                </span>
                {metric.proof_note && (
                  <span class="text-gray-500">{metric.proof_note}</span>
                )}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Stakeholder Feedback - Special Section -->
      <div class="mb-10">
        <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
          <span>{categories.feedback.icon}</span> {categories.feedback.label}
        </h2>
        
        <!-- Feedback context box -->
        <div class="p-5 rounded-xl bg-gradient-to-br from-purple-50 to-violet-50 dark:from-purple-900/20 dark:to-violet-900/20 border border-purple-200 dark:border-purple-800 mb-6">
          <p class="text-gray-700 dark:text-gray-300 mb-4">
            Over the years, I've collected feedback from colleagues, stakeholders, and leadership. Here's what the data shows:
          </p>
          <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
            {feedbackMeta.map((metric) => (
              <div class="text-center">
                <div class="text-2xl font-bold text-purple-600 dark:text-purple-400">{metric.value}</div>
                <div class="text-sm text-gray-600 dark:text-gray-400">{metric.label.replace('Feedback ', '').replace(' (over the years)', '')}</div>
              </div>
            ))}
          </div>
        </div>

        <!-- Theme bars -->
        <div class="p-5 rounded-xl bg-white dark:bg-gray-800/80 border border-gray-200 dark:border-gray-700">
          <h3 class="text-sm font-semibold text-gray-700 dark:text-gray-300 uppercase tracking-wide mb-4">
            Themes mentioned in feedback
          </h3>
          <p class="text-xs text-gray-500 dark:text-gray-500 mb-4">
            Percentage of feedback entries containing keywords related to each theme (keyword-based scan)
          </p>
          
          <div class="space-y-4">
            {feedbackThemes.map((theme) => {
              const themeName = theme.label.replace(' theme', '');
              const colors = themeColors[themeName] || { bg: 'bg-gray-500', text: 'text-gray-600 dark:text-gray-400' };
              const percentage = parseInt(theme.value.replace('~', '').replace('%', ''));
              return (
                <div>
                  <div class="flex justify-between items-center mb-1">
                    <span class={`font-medium ${colors.text}`}>{themeName}</span>
                    <span class="text-sm font-bold text-gray-900 dark:text-white">{theme.value}</span>
                  </div>
                  <div class="h-2 bg-gray-200 dark:bg-gray-700 rounded-full overflow-hidden">
                    <div 
                      class={`h-full ${colors.bg} rounded-full transition-all duration-500`}
                      style={`width: ${percentage}%`}
                    ></div>
                  </div>
                </div>
              );
            })}
          </div>
          
          <p class="mt-4 text-xs text-gray-500 dark:text-gray-500 italic">
            Keywords scanned include variations of: execution, delivery, technical, architecture, enable, coach, stakeholder, alignment, proactive, prepared, leadership, presence, etc.
          </p>
        </div>
      </div>

      <!-- Footer sections -->
      <div class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700">
        <div class="grid md:grid-cols-2 gap-8">
          <div>
            <h2 class="text-lg font-semibold text-gray-700 dark:text-gray-400 mb-4">What's intentionally omitted</h2>
            <ul class="space-y-2 text-gray-600 dark:text-gray-400 text-sm">
              <li>• Program names and internal project codenames</li>
              <li>• Customer or contract details</li>
              <li>• Proprietary financial figures</li>
              <li>• Anything that could identify individuals</li>
            </ul>
          </div>
          
          <div>
            <h2 class="text-lg font-semibold text-gray-700 dark:text-gray-400 mb-4">Notation</h2>
            <ul class="space-y-2 text-gray-600 dark:text-gray-400 text-sm">
              <li><code class="bg-gray-100 dark:bg-gray-800 px-2 py-0.5 rounded">~</code> — Approximate / directional</li>
              <li><code class="bg-gray-100 dark:bg-gray-800 px-2 py-0.5 rounded">+</code> — At least / minimum</li>
              <li>Ranges indicate variance across time</li>
            </ul>
          </div>
        </div>
      </div>

      <div class="mt-8 pt-8 border-t border-gray-200 dark:border-gray-700">
        <a href="/" class="inline-flex items-center gap-2 text-blue-600 dark:text-accent hover:underline">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
          </svg>
          Back to home
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
