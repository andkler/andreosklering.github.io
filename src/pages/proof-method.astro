---
import BaseLayout from '../layouts/BaseLayout.astro';
import metrics from '../data/metrics.json';
---

<BaseLayout title="Proof Method" description="How the metrics on this site are measured and validated.">
  <section class="py-12 md:py-16">
    <div class="max-w-2xl mx-auto px-6">
      <h1 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-4">
        How I measure
      </h1>
      
      <p class="text-lg text-gray-600 dark:text-gray-400 mb-8">
        Receipts over rhetoric means showing work. Here's how.
      </p>

      <div class="prose dark:prose-invert max-w-none">
        <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-4">Methodology</h2>
        
        <ul class="space-y-3 text-gray-700 dark:text-gray-300 mb-8">
          <li class="flex items-start gap-3">
            <span class="text-blue-600 dark:text-accent font-bold">→</span>
            <span><strong>Dashboard exports</strong> — Numbers from admin dashboards (Copilot usage, Auth0 analytics)</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-blue-600 dark:text-accent font-bold">→</span>
            <span><strong>Internal tools</strong> — Feedback tools, org charts, team trackers</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-blue-600 dark:text-accent font-bold">→</span>
            <span><strong>Keyword-based analysis</strong> — Theme percentages from scanning feedback text for relevant keywords</span>
          </li>
        </ul>

        <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-4">All metrics</h2>
        
        <div class="space-y-4">
          {metrics.all.map((metric) => (
            <div class="p-4 rounded-lg bg-gray-50 dark:bg-gray-800/50 border border-gray-200 dark:border-gray-700">
              <div class="flex justify-between items-start mb-2">
                <span class="font-medium text-gray-900 dark:text-white">{metric.label}</span>
                <span class="text-lg font-bold text-blue-600 dark:text-accent">{metric.value}{metric.unit && ` ${metric.unit}`}</span>
              </div>
              <div class="flex flex-wrap gap-2 text-xs">
                <span class="px-2 py-0.5 bg-gray-200 dark:bg-gray-700 rounded text-gray-600 dark:text-gray-400">
                  {metric.method}
                </span>
                {metric.proof_note && (
                  <span class="text-gray-500 dark:text-gray-500">{metric.proof_note}</span>
                )}
              </div>
            </div>
          ))}
        </div>

        {metrics.todo && metrics.todo.length > 0 && (
          <>
            <h2 class="text-xl font-semibold text-gray-900 dark:text-white mt-8 mb-4">Coming soon</h2>
            <ul class="space-y-2 text-gray-500 dark:text-gray-500">
              {metrics.todo.map((item) => (
                <li>• {item.label}</li>
              ))}
            </ul>
          </>
        )}

        <div class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700">
          <h2 class="text-lg font-semibold text-gray-700 dark:text-gray-400 mb-4">What's intentionally omitted</h2>
          
          <ul class="space-y-2 text-gray-600 dark:text-gray-400 text-sm mb-8">
            <li>• Program names and internal project codenames</li>
            <li>• Customer or contract details</li>
            <li>• Proprietary financial figures</li>
            <li>• Anything that could identify individuals in feedback</li>
          </ul>

          <h2 class="text-lg font-semibold text-gray-700 dark:text-gray-400 mb-4">Notation</h2>
          
          <ul class="space-y-2 text-gray-600 dark:text-gray-400 text-sm">
            <li><code class="bg-gray-100 dark:bg-gray-800 px-2 py-0.5 rounded">~</code> — Approximate / directional</li>
            <li><code class="bg-gray-100 dark:bg-gray-800 px-2 py-0.5 rounded">+</code> — At least / minimum</li>
            <li>Ranges indicate variance across time or measurement methods</li>
          </ul>
        </div>
      </div>

      <div class="mt-8 pt-8 border-t border-gray-200 dark:border-gray-700">
        <a href="/" class="inline-flex items-center gap-2 text-blue-600 dark:text-accent hover:underline">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
          </svg>
          Back to home
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
